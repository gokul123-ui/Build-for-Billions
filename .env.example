# JanConnect AI API Key Configuration (Optional)
# If provided, JanConnect uses the AI API for classification.
# If omitted or blank, JanConnect automatically falls back to its robust offline multilingual classifier.

VITE_AI_API_KEY=
